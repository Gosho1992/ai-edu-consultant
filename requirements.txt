openai>=1.12.0               # For GPT-4-turbo and Assistants API
python-dotenv>=1.0.0         # Load .env files (API keys)
requests>=2.31.0             # HTTP calls (e.g., for APIs and scraping)
streamlit>=1.32.0            # For the web UI (if you're using Streamlit)
fuzzywuzzy==0.18.0           # For fuzzy string matching (backend)
python-Levenshtein==0.12.2   # Speeds up fuzzywuzzy performance
feedparser==6.0.11           # For parsing RSS feeds
pdfplumber>=0.10.0           # For extracting text from PDFs
pytesseract>=0.3.10          # For OCR (images to text)
pillow>=10.0.0               # For image handling (used with pytesseract)
beautifulsoup4>=4.12.0       # For parsing HTML content
sentence-transformers>=2.2.2 # For semantic search/embedding matching
diskcache>=5.6.3             # For fast local caching
fastapi>=0.100.0             # For building the EduBot API
uvicorn>=0.24.0              # ASGI server to run FastAPI
scikit-learn>=1.3.0          # For cosine similarity
gspread
oauth2client
torch>=2.0.0



